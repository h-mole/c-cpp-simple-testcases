#include <stdio.h>

int main() {
    int a = -10;             // 有符号整数
    unsigned int b = 5;      // 无符号整数

    // 有符号整数除以无符号整数
    printf("有符号整数 -10 除以 无符号整数 5 的结果是: %u\n", a / b);

    return 0;
}
